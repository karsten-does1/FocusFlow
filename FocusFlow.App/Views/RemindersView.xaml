<UserControl
    x:Class="FocusFlow.App.Views.RemindersView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:FocusFlow.App"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:FocusFlow.App.ViewModels"
    d:DesignHeight="600"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  Header  -->
        <StackPanel
            Grid.Row="0"
            Margin="0,0,0,20"
            Orientation="Horizontal">
            <TextBlock
                VerticalAlignment="Center"
                FontSize="24"
                FontWeight="Bold"
                Text="Reminders" />
            <Button
                Margin="20,0,0,0"
                Padding="10,5"
                HorizontalAlignment="Left"
                Command="{Binding ShowNewReminderFormCommand}"
                Content="+ New Reminder" />
        </StackPanel>

        <!--  Filters  -->
        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <Button
                    Margin="0,0,10,0"
                    Padding="10,5"
                    Command="{Binding SetFilterAllCommand}"
                    Content="All" />

                <Button
                    Margin="0,0,10,0"
                    Padding="10,5"
                    Command="{Binding SetFilterUpcomingCommand}"
                    Content="Upcoming" />

                <Button
                    Margin="0,0,10,0"
                    Padding="10,5"
                    Command="{Binding SetFilterNextDaysCommand}"
                    Content="Next" />

                <ComboBox
                    Width="90"
                    Margin="0,0,10,0"
                    ItemsSource="{Binding NextDaysOptions}"
                    SelectedItem="{Binding SelectedNextDays, Mode=TwoWay}" />

                <TextBlock
                    Margin="0,0,10,0"
                    VerticalAlignment="Center"
                    Text="days" />

                <Button
                    Padding="10,5"
                    Command="{Binding SetFilterDoneCommand}"
                    Content="Done" />
            </StackPanel>

            <TextBlock
                Grid.Column="1"
                HorizontalAlignment="Right"
                VerticalAlignment="Center"
                Foreground="Gray"
                Text="{Binding ShownCount, StringFormat='{}{0} shown'}" />
        </Grid>

        <!--  Error Message  -->
        <TextBlock
            Grid.Row="2"
            Margin="0,0,0,10"
            Foreground="Red"
            Text="{Binding ErrorMessage}"
            Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVisibilityConverter}}" />

        <!--  Loading Indicator  -->
        <TextBlock
            Grid.Row="2"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Text="Loading..."
            Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}" />

        <!--  Reminders List  -->
        <ListBox
            Grid.Row="2"
            HorizontalContentAlignment="Stretch"
            ItemsSource="{Binding RemindersView}"
            Visibility="{Binding IsLoading, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border
                        Margin="0,5"
                        Padding="10"
                        HorizontalAlignment="Stretch"
                        BorderBrush="LightGray"
                        BorderThickness="1"
                        CornerRadius="5">
                        <Grid MinHeight="80">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <!--  Info  -->
                            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                <TextBlock
                                    FontWeight="Bold"
                                    Text="{Binding Title}"
                                    TextDecorations="{Binding Fired, Converter={StaticResource BooleanToTextDecorationConverter}}"
                                    TextTrimming="CharacterEllipsis"
                                    TextWrapping="NoWrap" />

                                <TextBlock Margin="0,6,0,0">
                                    <Run FontWeight="Bold" Text="Due: " />
                                    <Run Foreground="DarkBlue" Text="{Binding FireAtLocal, Mode=OneWay, StringFormat='{}{0:dd/MM/yyyy HH:mm}'}" />
                                </TextBlock>

                                <TextBlock Margin="0,2,0,0">
                                    <Run FontWeight="Bold" Text="Status: " />
                                    <Run Text="{Binding Fired, Converter={StaticResource BooleanToStringConverter}, ConverterParameter='Done|Pending'}" />
                                </TextBlock>
                            </StackPanel>

                            <!--  Actions  -->
                            <StackPanel
                                Grid.Column="1"
                                VerticalAlignment="Center"
                                Orientation="Horizontal">
                                <Button
                                    Margin="5,0"
                                    Padding="10,5"
                                    Command="{Binding DataContext.MarkDoneCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}"
                                    Content="Mark done"
                                    Visibility="{Binding Fired, Converter={StaticResource InverseBooleanToVisibilityConverter}}" />

                                <Button
                                    Margin="5,0"
                                    Padding="10,5"
                                    Background="LightCoral"
                                    Command="{Binding DataContext.DeleteReminderCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}"
                                    Content="Delete" />
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!--  Reminder Form (Overlay)  -->
        <Border
            Grid.Row="2"
            Width="420"
            Padding="20"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Background="White"
            BorderBrush="Black"
            BorderThickness="2"
            CornerRadius="10"
            Visibility="{Binding IsReminderFormVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel>
                <TextBlock
                    Margin="0,0,0,20"
                    FontSize="18"
                    FontWeight="Bold"
                    Text="New Reminder" />

                <TextBlock Margin="0,0,0,5" Text="Title *" />
                <TextBox Margin="0,0,0,15" Text="{Binding ReminderTitle, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Margin="0,0,0,5" Text="Fire Date &amp; Time" />

                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="10" />
                        <ColumnDefinition Width="90" />
                    </Grid.ColumnDefinitions>

                    <DatePicker Grid.Column="0" SelectedDate="{Binding ReminderFireDate, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                    <TextBox
                        Grid.Column="2"
                        VerticalContentAlignment="Center"
                        Text="{Binding ReminderFireTimeText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                        ToolTip="Time in format HH:mm (e.g. 09:30)" />
                </Grid>

                <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                    <Button
                        Margin="5,0"
                        Padding="15,5"
                        Command="{Binding CancelReminderFormCommand}"
                        Content="Cancel" />
                    <Button
                        Margin="5,0"
                        Padding="15,5"
                        Background="LightGreen"
                        Command="{Binding SaveReminderCommand}"
                        Content="Save" />
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
