<Window
    x:Class="FocusFlow.App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:FocusFlow.App"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="clr-namespace:FocusFlow.App.Views"
    xmlns:vm="clr-namespace:FocusFlow.App.ViewModels"
    Title="FocusFlow Assistant"
    Width="1200"
    Height="700"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!--  Sidebar Navigation  -->
        <Border
            Grid.Column="0"
            Background="#F5F5F5"
            BorderBrush="LightGray"
            BorderThickness="0,0,1,0">
            <StackPanel Margin="10">
                <TextBlock
                    Margin="0,20,0,30"
                    HorizontalAlignment="Center"
                    FontSize="20"
                    FontWeight="Bold"
                    Text="FocusFlow" />

                <Button
                    Margin="0,5"
                    Padding="10,8"
                    HorizontalContentAlignment="Left"
                    Command="{Binding NavigateToCommand}"
                    CommandParameter="Dashboard"
                    Content="📊 Dashboard" />

                <Button
                    Margin="0,5"
                    Padding="10,8"
                    HorizontalContentAlignment="Left"
                    Command="{Binding NavigateToCommand}"
                    CommandParameter="Tasks"
                    Content="📋 Tasks" />

                <Button
                    Margin="0,5"
                    Padding="10,8"
                    HorizontalContentAlignment="Left"
                    Command="{Binding NavigateToCommand}"
                    CommandParameter="Emails"
                    Content="📧 Emails" />

                <Button
                    Margin="0,5"
                    Padding="10,8"
                    HorizontalContentAlignment="Left"
                    Command="{Binding NavigateToCommand}"
                    CommandParameter="Reminders"
                    Content="⏰ Reminders" />

                <Button
                    Margin="0,5"
                    Padding="10,8"
                    HorizontalContentAlignment="Left"
                    Command="{Binding NavigateToCommand}"
                    CommandParameter="Settings"
                    Content="⚙️ Settings" />
            </StackPanel>
        </Border>

        <!--  Main Content Area  -->
        <ContentControl Grid.Column="1" Content="{Binding CurrentViewModel}">
            <ContentControl.Resources>
                <DataTemplate DataType="{x:Type vm:DashboardViewModel}">
                    <views:DashboardView DataContext="{Binding}" />
                </DataTemplate>
                <DataTemplate DataType="{x:Type vm:TasksViewModel}">
                    <views:TasksView DataContext="{Binding}" />
                </DataTemplate>
                <DataTemplate DataType="{x:Type vm:EmailsViewModel}">
                    <views:EmailsView DataContext="{Binding}" />
                </DataTemplate>
                <DataTemplate DataType="{x:Type vm:RemindersViewModel}">
                    <views:RemindersView DataContext="{Binding}" />
                </DataTemplate>
                <DataTemplate DataType="{x:Type vm:SettingsViewModel}">
                    <views:SettingsView DataContext="{Binding}" />
                </DataTemplate>
            </ContentControl.Resources>
        </ContentControl>
    </Grid>
</Window>
